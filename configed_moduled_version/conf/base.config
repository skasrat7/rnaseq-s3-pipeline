// Base configuration for all profiles
// This file can be empty or contain general settings

process {
    // General process settings that apply to all profiles
    shell = ['/bin/bash', '-euo', 'pipefail']
}
